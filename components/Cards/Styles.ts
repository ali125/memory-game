import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexWrap: "wrap",
    gap: 10,
    flexDirection: "row",
    padding: 10,
    marginBottom: 10,
  },
});

export default styles;
